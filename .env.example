# FEK 配置 —— 复制为 .env 后按需修改。
# 默认模式为 "mock"：整个 Demo 离线运行，无需任何 API key。

# 后端选择："mock"（默认，离线）或 "openai"
FEK_MODE=mock

# 仅当 FEK_MODE=openai 时使用
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
